import React from 'react';

import { Container, Search, User } from './styles';

const Menu = () => (
  <Container>
    <Search>
      <input placeholder="Pesquisar" />
    </Search>
  </Container>
);

export default Menu;
